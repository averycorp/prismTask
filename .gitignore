# Gradle
.gradle/
build/
local.properties

# Worktrees — local isolated checkouts created via `git worktree add`.
# These are not meant to be tracked from the parent checkout.
.worktrees/
worktrees/

# Build outputs are produced by CI — do not commit
# app/build/ is already covered by build/ above

# Kotlin
.kotlin/
.kotlin/errors/

# IDE
.idea/
!.idea/codeStyles/
*.iml
.vscode/

# Android
*.ap_
captures/

# React web app
web/node_modules/
web/dist/
web/dist-ssr/

# Python
__pycache__/
*.pyc
.env
.venv/
*.egg-info/
.pytest_cache/
test.db
test.db-shm
test.db-wal
backend/junit.xml

.keystores/

# Signing keys and credentials
*.keystore
*.jks
keystore_base64.txt
# Exception: the debug keystore is committed so all CI runs (and local
# builds) produce APKs with a stable signature, which is required for
# in-app updates to pass the installed-signature check.
!app/debug.keystore

# macOS
.DS_Store

# Claude Code
.claude/*
!.claude/commands/
!.claude/commands/**

# Build artifacts in repo root
*.apk
!app-debug.apk
*.aab
apk/*.apk
apk/*.aab

# Stray test/log artifacts
test.json
sync-*.json
parse-test.json
simple-test.json
build-log*.txt
compile-log.txt

# Nested bare git repo
averyTask.git/

# Firebase emulator data + logs
firebase-emulator-data/
firebase-debug.log
firestore-debug.log
ui-debug.log
audit/

# Local-only artifacts from sync audit — never commit
# (service-account keys = full admin access; DB dumps = user data)
prismtask-sa.json
*-firebase-adminsdk-*.json
averytask.db
averytask.db-shm
averytask.db-wal

# Fix D working snapshot (read-only DB copies)
.fixd-workspace/

# Local dev tooling (gh CLI, etc.)
.tools/

# Firestore ad-hoc audit / dedup output reports and logs (scripts are committed; generated reports are not)
firestore_audit_v*.md
firestore_state_check_*.md
dedup_*.log
/secrets
test-results/
playwright-report/
venv/
